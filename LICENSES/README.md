# License scope

- `BSD-2-Clause.txt` (the repository's `LICENSE` in the source tree) applies to
  the files authored for this repository.
- `ZSH.txt`, copied from the pinned zsh source during packaging, applies to zsh
  except where an individual file states otherwise.
- `GPL-2.0.txt` applies to the `_darcs`, `_osc`, `_qdbus`, and `_zypper`
  completion functions included by zsh's `install.fns` target.
- `NCURSES.txt` applies to the ncurses terminfo entries copied into the
  cryptex.
